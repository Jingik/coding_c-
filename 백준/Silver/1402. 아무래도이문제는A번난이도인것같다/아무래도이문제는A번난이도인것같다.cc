#include <iostream>

using namespace std;

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr); cout.tie(nullptr);
    int t;
    int a,b;
    
    cin >> t;
    while(t--)
    {
        cin >> a >> b;
        cout << "yes" << endl;
    }
    return 0;
}
